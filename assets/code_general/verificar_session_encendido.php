<?php
/*    if (isset($_SESSION['id_usuario'])) {
        session_unset(); 
        session_destroy(); 
        header("Location: /index.php");
        exit;
    }
    // Verifica que la sesion este encendida en paginas donde no deveria, borra la sesion y redirecciona a la pagina de inicio*/
?>